# Test environment (copy to .env.test or set when running E2E tests)
# Used when running E2E tests against a real Supabase instance.

# Supabase (test project or local)
NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_ANON_KEY=

# Optional: run E2E tests against a running app
# E2E_BASE_URL=http://localhost:3000

# Optional: cron secret for testing /api/cron/bills
# CRON_SECRET=your-test-cron-secret
