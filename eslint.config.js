// eslint-config-next v16 ships native flat config — require it directly.
// This replaces the legacy .eslintrc.json that used the old "extends" format.
const nextConfig = require('eslint-config-next/core-web-vitals')

module.exports = nextConfig
